{
  "name": "sentrail",
  "version": "0.1.1",
  "description": "Sentrail local interceptor — enforce agent governance policies on AI coding agent tool calls before they execute.",
  "type": "module",
  "bin": {
    "sentrail": "dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/cli.js",
    "test": "npm run build && tsc -p test/tsconfig.json && node --test dist-test/*.test.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "sentrail",
    "mcp",
    "ai-agents",
    "governance",
    "claude-code",
    "cursor",
    "codex"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
